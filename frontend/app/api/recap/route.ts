import { NextRequest, NextResponse } from "next/server";
export async function GET(request:NextRequest){const base=process.env.APPS_SCRIPT_API_URL;if(!base)return NextResponse.json({success:false,data:null,message:"APPS_SCRIPT_API_URL belum dikonfigurasi."},{status:500});const url=new URL(base);url.searchParams.set("action","getRecap");for(const key of ["date_from","date_to"]){const v=request.nextUrl.searchParams.get(key);if(v)url.searchParams.set(key,v)}try{return NextResponse.json(await fetch(url,{cache:"no-store"}).then(r=>r.json()))}catch{return NextResponse.json({success:false,data:null,message:"Ringkasan gagal terhubung ke backend."},{status:500})}}

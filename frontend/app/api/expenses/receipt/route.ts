import { NextRequest, NextResponse } from "next/server";
export async function POST(request:NextRequest){const base=process.env.APPS_SCRIPT_API_URL;if(!base)return NextResponse.json({success:false,data:null,message:"APPS_SCRIPT_API_URL belum dikonfigurasi."},{status:500});let body;try{body=await request.json()}catch{return NextResponse.json({success:false,data:null,message:"Permintaan tidak valid."},{status:400})}const url=new URL(base);url.searchParams.set("action","addExpenseReceipt");try{return NextResponse.json(await fetch(url,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify(body),cache:"no-store"}).then(r=>r.json()))}catch{return NextResponse.json({success:false,data:null,message:"Receipt gagal terhubung. Expense tetap tersimpan."},{status:500})}}

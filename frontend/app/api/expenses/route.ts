import { NextRequest } from "next/server"; import { backendUrl, bodyWithSession, fail, forwardBackend } from "@/lib/backend";
export async function GET(request:NextRequest){const params:Record<string,string>={};for(const k of ["date_from","date_to"]){const v=request.nextUrl.searchParams.get(k);if(v)params[k]=v}const result=await backendUrl("listExpenses",params);if("error" in result)return result.error;return forwardBackend(result.url)}
export async function POST(request:NextRequest){let body:unknown;try{body=await request.json()}catch{return fail("Permintaan tidak valid.",400)}const result=await backendUrl("createExpense");if("error" in result)return result.error;return forwardBackend(result.url,{method:"POST",headers:{"Content-Type":"application/json"},body:bodyWithSession(body,result.token)})}
